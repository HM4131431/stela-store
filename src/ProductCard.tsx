import type{Product}from'./products';export function ProductCard({product,onClick}:{product:Product;onClick?:()=>void}){return <div onClick={onClick} className="group flex flex-col border border-neutral-200 rounded-lg overflow-hidden transition-all duration-300 hover:border-neutral-400 hover:shadow-md cursor-pointer"><div className="aspect-square bg-neutral-100 overflow-hidden"><img src={product.image} alt={product.name} loading="lazy" className="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"/></div><div className="flex flex-col gap-2 p-5"><h3 className="font-serif text-xl text-neutral-900 text-center">{product.name}</h3>{product.description&&<p className="text-sm text-neutral-500 text-center leading-relaxed">{product.description}</p>}<p className="text-sm text-neutral-700 text-center mt-1">{product.price}</p></div></div>}export function ProductGrid({products,onProductClick}:{products:Product[];onProductClick?:(p:Product)=>void}){return <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 sm:gap-8">{products.map(p=><ProductCard key={p.id} product={p} onClick={()=>onProductClick?.(p)}/>)}</div>}
