import{ArrowRight}from'lucide-react';import type{Product,ProductCategory}from'./products';import{CATEGORIES,getProductsByCategory}from'./products';import{ProductGrid}from'./ProductCard';export function ProductCategoryPage({category,onBackToProducts,onBackToMenu}:{category:ProductCategory;onBackToProducts:()=>void;onBackToMenu:()=>void}){const info=CATEGORIES.find(c=>c.slug===category),products:Product[]=getProductsByCategory(category);return <div className="min-h-screen bg-white px-6 py-10 sm:py-16"><div className="max-w-5xl mx-auto"><button onClick={onBackToMenu} className="flex items-center gap-2 text-neutral-500 text-sm hover:text-neutral-900 mb-10"><ArrowRight size={18}/>بازگشت</button><h1 className="font-serif text-3xl sm:text-4xl text-neutral-900 text-center mb-4">{info?.name||'محصولات'}</h1>{info?.description&&<p className="text-neutral-500 text-center mb-12">{info.description}</p>}<div className="flex justify-center mb-12"><button onClick={onBackToProducts} className="flex items-center gap-2 text-sm text-neutral-600 hover:text-neutral-900"><ArrowRight size={16}/>بازگشت به محصولات</button></div>{products.length?<ProductGrid products={products}/>:<p className="text-center text-neutral-400 font-serif text-lg">به‌زودی محصولات این دسته اضافه می‌شوند</p>}</div></div>}
