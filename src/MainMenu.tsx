import{ShoppingBag,ShoppingCart,Phone}from'lucide-react';
type MenuOption={icon:typeof ShoppingBag;label:string;screen:'products'|'cart'|'contact'};
const OPTIONS:MenuOption[]=[{icon:ShoppingBag,label:'محصولات',screen:'products'},{icon:ShoppingCart,label:'سبد خرید',screen:'cart'},{icon:Phone,label:'تماس با ما',screen:'contact'}];
export default function MainMenu({onNavigate}:{onNavigate:(s:'products'|'cart'|'contact')=>void}){return <div className="flex flex-col sm:flex-row items-center justify-center gap-16 sm:gap-24 md:gap-32">{OPTIONS.map(({icon:Icon,label,screen},i)=><button key={label} onClick={()=>onNavigate(screen)} className="group flex flex-col items-center gap-5 cursor-pointer opacity-0 animate-[fadeInUp_.6s_ease-out_forwards]" style={{animationDelay:`${.3+i*.15}s`}}><span className="w-40 h-40 flex items-center justify-center rounded-full border border-neutral-200 text-neutral-800 transition-all duration-300 group-hover:scale-110 group-hover:border-neutral-900 group-hover:shadow-lg"><Icon size={60} strokeWidth={1.5}/></span><span className="font-serif text-lg text-neutral-800 tracking-wide">{label}</span></button>)}</div>}
